-- Type aliases used in LSP annotations

--- @alias opt { minecraft_dir: 'infer'|filepath, nbted_command: string, auto_detect_nbt: boolean, enable_commands: boolean, auto_encode: boolean, backup_on_encode: boolean, verbose: boolean, detect_nbt: 'auto'|fun(filename: filepath): boolean }
--- @alias filepath string
